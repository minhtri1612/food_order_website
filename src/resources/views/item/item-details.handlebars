<div class="container2">
    <section class="menu-section">
        <div class="details-container listProduct">
            <div class="image-container">
                <img src="{{item.image}}" alt="{{item.name}}" class="image-details">
            </div>
            <div class="info-container">
                <h1>{{item.name}}</h1>
                <p>{{item.description}}</p>
                
                <p>Price: ${{item.price}}</p>
                <a  class="btn btn-primary "
                    data-id="{{item.id}}"
                    data-name="{{item.name}}"
                    data-image="{{item.image}}"
                    data-price="{{item.price}}"
                    >
                    Order Now
                </a>
            </div>
        </div>
    </section>
</div>

<style>
    body.item-details-bg {
        background: url('/img/restaurants_background.jpg') no-repeat center center fixed;
        background-size: cover;
        
    }
    .image-details {
        width: 500px;
        height: 500px;
        box-shadow: 10px 10px 10px rgba(0, 0, 0, 0.3);
        border-radius: 15px;
        
        
    }
    .details-container {
        display: flex;
        justify-content: space-evenly;
        align-items: flex-start;
        gap: 40px;
        
        
    }
    .info-container .btn-primary {
        background: #ffd95e;
        border: none;
        color: #fff;
        padding: 12px 30px;
        border-radius: 25px;
        font-size: 1.6rem;
        font-weight: 600;
        transition: background-color 0.3s ease;
        }
    .btn-primary:hover {
        background: #e0a800;
    }
    
    .details-container .image-container {
      
    }

    .info-container {
        background: #fffbe8;
        border-radius: 18px;
        box-shadow: 0 4px 24px rgba(0,0,0,0.08);
        padding: 36px 32px;
        min-width: 320px;
        max-width: 400px;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        gap: 18px;
    }

    .info-container h1 {
        font-size: 2.2rem;
        font-weight: 700;
        margin-bottom: 8px;
        color: #333;
    }

    .info-container p {
        font-size: 1.2rem;
        color: #555;
        margin-bottom: 8px;
    }


</style>
